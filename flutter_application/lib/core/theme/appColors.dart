import 'package:flutter/material.dart';

abstract class AppColors {
  // Backgrounds
  static const Color background = Color(0xFF050B10);
  static const Color surface = Color(0xFF1C242C);
  static const Color surfaceElevated = Color(0xFF232D37);

  // Brand
  static const Color primary = Color(0xFF00C853);
  static const Color primaryDark = Color(0xFF009624);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white54;
  static const Color textHint = Colors.white38;

  // Icon
  static const Color iconDefault = Colors.white54;

  // Misc
  static const Color logoBackground = Colors.white;
  static const Color divider = Color(0xFF2A3540);
}